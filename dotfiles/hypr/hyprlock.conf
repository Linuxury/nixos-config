# ===========================================================================
# hyprlock.conf — Screen locker
#
# Background: blurred screenshot of the current screen.
# Center: large clock + date label + password input field.
# Colors: sourced from matugen-generated colors-hyprlock.conf
# ===========================================================================

source = ~/.config/hypr/colors-hyprlock.conf

general {
    disable_loading_bar = true
    hide_cursor         = true
    grace               = 0
    no_fade_in          = false
}

# ---------------------------------------------------------------------------
# Background — screenshot with blur
# ---------------------------------------------------------------------------
background {
    monitor    =
    path       = screenshot
    blur_passes = 3
    blur_size   = 8
    brightness  = 0.5
    contrast    = 0.9
    vibrancy    = 0.2
    vibrancy_darkness = 0.0
}

# ---------------------------------------------------------------------------
# Clock — large, centered above input
# ---------------------------------------------------------------------------
label {
    monitor   =
    text      = cmd[update:1000] echo "$(date +"%H:%M")"
    color     = $on_surface
    font_size = 80
    font_family = JetBrainsMono Nerd Font Propo

    position = 0, 120
    halign   = center
    valign   = center
}

# ---------------------------------------------------------------------------
# Date — smaller, just below clock
# ---------------------------------------------------------------------------
label {
    monitor   =
    text      = cmd[update:60000] echo "$(date +"%A, %B %d")"
    color     = $on_surface_var
    font_size = 18
    font_family = JetBrainsMono Nerd Font Propo

    position = 0, 30
    halign   = center
    valign   = center
}

# ---------------------------------------------------------------------------
# Password input field
# ---------------------------------------------------------------------------
input-field {
    monitor =

    size             = 280, 52
    outline_thickness = 2
    dots_size        = 0.25
    dots_spacing     = 0.15
    dots_center      = true

    outer_color  = $primary
    inner_color  = $surface
    font_color   = $on_surface
    check_color  = $tertiary
    fail_color   = $error

    rounding         = 10
    fade_on_empty    = true
    placeholder_text = <i>Password...</i>
    hide_input       = false

    fail_text        = <i>Wrong — $ATTEMPTS attempt(s)</i>

    position = 0, -80
    halign   = center
    valign   = center
}
