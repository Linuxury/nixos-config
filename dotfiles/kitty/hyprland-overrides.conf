# ===========================================================================
# kitty/hyprland.conf — Hyprland-specific overrides
#
# Only loaded when Hyprland is the active compositor.
# Hyprland handles window transparency and blur at the compositor level,
# so Kitty's own background_opacity and background_blur are redundant.
# ===========================================================================

background_opacity  1.0
background_blur     0
